Category = "Unfiled"; Secret = 0
Places = 3; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Slow-path finds honest no-root error via multi-sub z-direction"
"Main solve finds x=2.236, z=4.472 (solution of the system)."
"x[0:2]-> fast path rejects (2.236 not in [0,2]). Slow path promotes"
"x to INPUT+[0:2]. The sweep-subs filter excludes x (INPUT+limits),"
"but multi-sub derivation yields z-direction subs too (z → 2*x and"
"z → sqrt(25-x²)). Kind 3 applies z → 2*x to eq2: x² + (2x)² = 25"
"→ 5x² = 25 → 1-unknown in x. Brent's tries [0, 2], no root, reports"
"'Could not find a root in range [0:2] for 'x'' honestly."
"x[0:3]-> takes the fast path (2.236 in [0,3]) and is unaffected."

x = z/2
x**2 + z**2 = 25

--Variables--
z->
x[0:3]->
x[0:2]->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 3; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Same system but with abs(x) reports honest 'no root' error"
"Identical system to the record above, but with `abs(x) = z/2`"
"instead of `x = z/2`. deriveSubstitution can't isolate x from"
"abs(x) on the LHS, so it inverts the other way: sub `z → abs(x)*2`"
"for z (not x). Slow path promotes x to INPUT+[0:2]; sweep-subs"
"still excludes x (INPUT+limits), but z IS included (no limits)."
"Kind 3 applies z → abs(x)*2 to eq2: x² + 4·abs(x)² = 25 → 5x² = 25"
"→ one-unknown in x. Brent's tries [0, 2], finds no root (sqrt(5) ≈"
"2.236 outside the window) → 'Could not find a root in range [0:2]'."
"Tiny syntactic change (x vs abs(x) on LHS) flips which variable"
"has a sub, which determines whether slow path can reduce."

abs(x) = z/2
x**2 + z**2 = 25

--Variables--
z->
x[0:2]->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 3; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"OUTPUT vs INPUT 'has no value to output' asymmetry"
"Brent's fails for x[0:2]<- (real root 2.236 outside [0,2]). z can't"
"be derived without x. z-> gets 'has no value to output' error (OUTPUT"
"requires a value to display). z<- with no value gets NO error (INPUT"
"placeholder semantics: empty is OK). Same logic for x->: in"
"solveFailures from Brent's, so its specific 'Could not find a root'"
"error fires and the generic 'has no value' is suppressed."

x**2 + z**2 - w = 25
x = z/2
// w = 0
--Variables--
w<- 0
// z[0:4]->
// x[0:3]-> 2.24
x[0:2]<-
x->
// x<- 2.24
// z<- 4.47
z->
z<-
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 3; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Table transitive blanking: z derived from bad x should also blank"
"Equations: x*x = y (solves x = sqrt(y)), x = x + 1 (always fails),"
"x = z (propagates x to z). For each row iterating y, x is body-"
"derived and goes bad on `x = x + 1`. Transitive propagation: `x = z`"
"contains the bad x, so z (also body-derived) is marked bad too."
"Result: x and z blanked; y shown honestly (iterator)."

y: 2
x*x = y
x = x + 1
x = z

--Variables--
x-> 1.414
z-> 1.414

table("transitive") = {
  y: 0..4..1
  y->
  x->
  z->
}
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Equation oddities: incomplete equations and missing sides"
"`a =` is the PalmOS-legacy evaluate-LHS pattern: the value of a gets"
"inserted right after the =. A trailing comment is preserved (value"
"goes before //, not at end of line). `= expr` and bare `=` are"
"flagged as parse errors — 'Equation missing left side' — so the user"
"notices the typo rather than silently dropping the line."

a: 1

a =  // test

b: 1
 = b
=
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Self-referencing equation with unbound var: 'Unknown in equation' (singular)"
"`a = a` is structurally a tautology, but with a unbound the balance"
"loop can't verify it. Reports 'Unknown in equation: a' (singular form,"
"since the equation has exactly one unknown), marks a as unsolved."
"`b:` is an INPUT placeholder not referenced by any equation — no"
"status assigned, no error."

a:
a = a
b:
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"priorBalanced shield: a is gray (in earlier balanced eq), e1 is orange"
"Line 3 `a + b = 25` balances with a=10, b=15 (the verified balanced"
"equation). Line 4 `d + e1 = a + 5` becomes firstFailedEq because"
"d and e1 are unknown. The partial/unsolved highlight pass: a appears"
"in priorBalanced (line 3 balanced it) AND in firstFailedEq's allVars"
"→ marked 'partial' (gray). e1 in firstFailedEq but NOT in priorBalanced"
"→ marked 'unsolved' (orange). b isn't in firstFailedEq.allVars so"
"gets no status (it's verified in line 3 but the green branch is"
"gated off when firstFailedEq is set)."

a + b = 25
d + e1 = a + 5

--Variables--
a: 10
b: 15
e1:
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Mixed example: two independent components, one solves and one doesn't"
"Component A {y = x + 1 + c2}: y, x known → Brent's solves c2 = 0,"
"equation balances → y, x marked green via the green branch."
"Component B {d + e1 = a + b + 5, a + b = 25}: a, b known, d/e1 unknown."
"Line 4 sets firstFailedEq (unknowns), so a, b → 'unsolved' (orange)."
"Line 5's balance failure (15 ≠ 25) doesn't emit because firstFailedEq"
"is already set (focus rule). Each component independently determines"
"its own highlights and errors."

y = x + 1 + c2
d + e1 = a + b + 5
a + b = 25

--Variables--
y: 6
x: 5
a: 10
b: 5
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Undeclared-var definition propagates partial highlight via expandVars"
"`z = y + a` defines undeclared z. definitionDeps['z'] = {y, a} —"
"z's underlying vars. `z = 2` then fails balance (z is 8, not 2)."
"firstFailedEq = line 8 (z = 2). expandVars({z}) → {y, a}. Both"
"appear in priorBalanced (line 2's `a = x + y` balanced) → both"
"marked 'partial' (gray). This is the 'volume = w*h*d' pattern:"
"when the undeclared label's equation fails, the underlying declared"
"vars get the highlight (since the label has no var-panel row)."

a = x + y
x: 2
y: 3
a: 5

z = y + a
z = 2
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 4; StripZeros = 1
Format = "float"; GroupDigits = 1; DegreesMode = 0; ShadowConstants = 0
"Body def error surfaces even when solver binds the var"
"actYears is declared with body-def value `years`. The solver still"
"binds actYears via Brent's on the pmt equation (the model doesn't"
"block solver paths). But the body def itself can't evaluate (years"
"has no value), so reportUnevaluableBodyDefs emits 'Cannot evaluate"
"years'. actYears retains its solver-derived value; the error makes"
"the body-def's silent failure visible."

"Loan calculator with graphs and tables"

--Functions--
pmt2(pv; fv; rate; n; pmtDue) = -(pv + fv/(1 + rate)**n)*rate / ((1 - (1 + rate)**-n)*(1 + rate)**pmtDue)

--Equations--
// pmt = pmt2(pv; fv; r; years*pmtsYr; pmtDue)
pmt + extraPmt = pmt2(pv; fv; r; actYears*pmtsYr; pmtDue)
r = (1 + rate/cmpndsYr)**(cmpndsYr/pmtsYr) - 1
// these variables never get orange highlighting because they are "solved" here
pmtDue = pmtDue
cmpndsYr = cmpndsYr
pmtsYr = pmtsYr
// extraPmt = extraPmt
// actYears = actYears

--Hidden Variables--
lastPmt: round(actYears*pmtsYr - pmtDue)
totPmt: pmt + extraPmt
begin: 1
end: 0

// Begin variables (top) panel
--Variables--
"*Update values, then click solve or the solve ⟲ icon next to a highlighted variable"
"For example, set Present Value to $200,000 and press the solve ⟲ icon next to Payment"

Present Value pv $: $100,000
Future Value fv $: $0               "(balloon payment)"
Annual Rate rate %: 6.125%
Nominal Years years :
Payment pmt $: -$607.61


Extra Payment extraPmt $: $0        "Extra principal payment per period"
Actual Years actYears : years       "Actual years given extra principal payment"


Payments/Year pmtsYr: 12
Compounds/Year cmpndsYr: pmtsYr     "generally equals payments/year"
Annuity Due pmtDue[0..1]: end       "end or begin of period"
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Solver binds d via equation; body def's failure surfaces as error"
"`d: c` declares d's value as the expression `c`. The equation `d = 12`"
"binds d via direct-eval (the solver isn't blocked by the body def)."
"Body def `c` can't evaluate (c is unbound), so 'Cannot evaluate c -"
"Variable c has no value' is emitted. d retains the solver value 12;"
"the error documents that the body def couldn't be honored."

d: c
d = 12
d->
c->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Body def fires after solver binds; overrides when expression differs"
"Solver binds the LHS via the equation, then phase [1] fires the body"
"def. The body def's evaluated value overrides the solver's value."
""
"`r: r` + `r = 0`: solver sets r=0, body def evaluates r → 0, no change."
"No errors."
""
"`s: s + 1` + `s = x + y**2`: solver sets s=5, body def evaluates s+1 → 6"
"and overrides s. The balance loop then catches the conflict between"
"the equation (s should be 5) and the overridden value (6) — surfaced"
"as a balance error."

x: 1
y: 2

z = x + y**2

r = 0
r: r
r-> 0

s = x + y**2

s: s + 1
s->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Solver binds when it can; body-def errors surface independently"
"Solver sets z = 5 from `z = x + y**2`. Body def `z+w` can't fire"
"(w unbound), so 'Cannot evaluate z+w - Variable w has no value' is"
"emitted; z keeps its solver value."
""
"z1 has no equation, so the solver has no path to bind it. Its body"
"def `z+q` can't evaluate (q is unbound), so z1 stays unbound and"
"the same 'Cannot evaluate' error fires for it. w and q both stay"
"unbound with no-value-to-output errors on their OUTPUT lines."

x: 1
y: 2
z = x + y**2

z: z+w
z->
w->

z1: z+q
z1->
q->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0; ShadowConstants = 0
"Body def re-evaluates to same value as equation — no conflict"
"Solver sets z = 5 from `z = x + y**2` and d = 2 from `d = 2`. Once"
"both deps (z and d) are in context, phase [1] fires the body def:"
"z**d/z = 5**2/5 = 5. Same value, no override, no balance error."

x: 1
y: 2
z = x + y**2

d = 2
z: z**d/z
z->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 4; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0
"Body-def override propagates to live expression output but not to solver-set w"
"`z: z + 1` overrides z = 5 (from equation) → z = 6 in phase [1]. The"
"balance loop then catches the conflict on `z = sqrt(x²+y²)` (5 vs 6)."
"The live expression output `sqrt(z*z+x*x+y*y)->` evaluates with the"
"current (post-override) z = 6 → 7.8102."
""
"BUT w was set by direct-eval in the prior pass using z = 5, giving"
"w = sqrt(50) ≈ 7.0711. The `w: w` body def is a no-op (evaluates to"
"current w) and doesn't re-derive from the equation. So w is stale."
"This documents the known propagation gap: solver-set vars derived"
"from a value that a body def later overrides are not invalidated."

x: 3
y: 4
z = sqrt(x**2 + y**2)
z: z + 1
z->

w = sqrt(z*z + x*x + y*y)
w: w
w->

sqrt(z*z + x*x + y*y)->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 4; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 0
"Solver chains through z and q; only the body def's error is reported"
"Solver sets z = 5 from `z = y + 3` via direct-eval. Then q = z + 1 = 6"
"from `q = z + 1`. The body def `z + w` can't fire (w unbound), so"
"'Cannot evaluate z+w - Variable w has no value' is emitted. z and q"
"retain their solver values; only w stays unbound."

y: 2

z = y + 3
q = z + 1

z: z + w
z->
w->
q->
~~~~~~~~~~~~~~~~~~~~~~~~~~~
Category = "Unfiled"; Secret = 0
Places = 2; StripZeros = 1
Format = "float"; GroupDigits = 0; DegreesMode = 1
"Body def fires after deps bind; overrides solver-set z"
"Direct-eval sets z = 5 from `z = x + y**2`. Body def `z+w` can't fire"
"yet — w is unbound. Brent's (Kind 2 sweep0) solves `cos(w) = sin(x +"
"y**2)` for w numerically — w is its only unknown — and binds w = 85°."
"(tryIsolateVariables only handles binary ops, so cos can't be algebrai-"
"cally inverted; Brent's does it.) Now phase [1] fires z's body def:"
"5 + 85 = 90, which overrides z. The balance loop catches the conflict"
"on the z equation (5 ≠ 90)."

x: 1
y: 2
z = x + y**2

z: z + w
z-> 90
w-> 85

cos(w) = sin(x + y**2)
~~~~~~~~~~~~~~~~~~~~~~~~~~~
